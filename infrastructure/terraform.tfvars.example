# Scaleway Medical AI Lab - Example Variables
# Copy this file to terraform.tfvars and fill in your values (OpenTofu uses the same file names):
#   cp terraform.tfvars.example terraform.tfvars

access_key      = "SCWXXXXXXXXXXXXXXXXX"
secret_key      = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
organization_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
project_id      = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# Your unique student identifier (lowercase, no spaces)
student_id = "student-01"

# Domain name for Let's Encrypt TLS (optional).
# Leave commented out for HTTP-only deployment (no domain needed).
# If set: create a DNS A record pointing to the LB IP *before* `tofu apply`.
# domain_name = "lab.cloudaura.io"
